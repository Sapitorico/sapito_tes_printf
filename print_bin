int print_bin(va_list arg)
{
        long int bin = 0;
        int remainder;
        int i = 0;

        bin = va_arg(arg, int);
        while (bin != 0)
        {
                remainder = bin % 2;
                bin /= 2;
                bin += remaider * i;
                i *= 10;
        }
}
